include ../assets/pug/blocks.pug


<!DOCTYPE html>
html(lang="zxx")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Headers$Footers
  body
      +header({entered: false})
      +header({entered: true})
      +footer()
      +copyright
